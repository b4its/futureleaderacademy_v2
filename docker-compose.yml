services:
  db:
    image: mysql:8.4
    container_name: flacademyv2-db
    environment:
      MYSQL_DATABASE: flacademy
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - flacademyv2-network

  # GRUP APACHE 
  apache:
    build:
      context: .
      dockerfile: ./docker-apache/Dockerfile
    container_name: flacademyv2-apache
    profiles: ["apache"] # <--- Hanya jalan jika dipanggil
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
    networks:
      - flacademyv2-network

  # GRUP NGINX (Terdiri dari Nginx + PHP-FPM)
  php-fpm:
    build:
      context: .
      dockerfile: ./docker-nginx/Dockerfile
    container_name: flacademyv2-php-fpm
    profiles: ["nginx"] # <--- Ikut grup nginx
    volumes:
      - .:/var/www/html
    networks:
      - flacademyv2-network

  nginx:
    image: nginx:alpine
    container_name: flacademyv2-nginx
    profiles: ["nginx"] # <--- Ikut grup nginx
    ports:
      - "8000:80"
      - "5173:5173" # misal pakai vite
    volumes:
      - .:/var/www/html
      - ./docker-nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - php-fpm
    networks:
      - flacademyv2-network

networks:
  flacademyv2-network:
    driver: bridge

volumes:
  mysql_data: